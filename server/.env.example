# Copie este arquivo para ../.env ou configure suas variáveis no .env na raiz do projeto
# Exemplo de variáveis usadas pelo Configuracao/database.js
DB_NAME=seu_db
DB_USER=seu_usuario
DB_PASS=sua_senha
DB_HOST=localhost
DB_PORT=5432

# Chave JWT (troque em produção)
JWT_SECRET=troque_para_uma_chave_secreta

# Porta do servidor de autenticação
PORT_SERVER=4000
